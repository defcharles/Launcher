import React, { useState } from "react";
import { Option } from "../Option";

export const DeveloperPage = () => {
  return (
    <div className="flex flex-col gap-3 text-white/80">
      <h2 className="text-lg font-semibold text-red-400">Developer Settings</h2>
    </div>
  );
};
